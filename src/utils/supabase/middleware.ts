// Vite/React app: use the shared Supabase browser client.
// This file exists only to avoid Next.js-specific imports.
import { supabase } from "@/integrations/supabase/client";

export { supabase };

